# Runtime dependencies. Pinned to lower bounds that were actually exercised;
# see docs/ for the versions each result was produced with.

# Detection and tracking
ultralytics>=8.4.118
torch>=2.2.2

# Pose estimation
mediapipe>=0.10.21

# Imaging and numerics
opencv-python>=4.9
numpy>=1.26

# Configuration
PyYAML>=6.0

# Notebooks and figures
matplotlib>=3.8
jupyter>=1.0
